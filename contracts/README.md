# Contracts

Machine-readable interface contracts. Nothing here yet.

Planned:

- `pinmap.yaml`: every Teensy pin, its signal, direction and the device it connects to. Generates the firmware header and is read by the ROS bridge.
- `serial_protocol.md`: framed serial protocol between Jetson and Teensy, versioned.
- `ros_interfaces.yaml`: topics, message types, rates, QoS, owners.
- `estop_power_chain.drawio`: the E-stop and power-meter wiring, verified with a meter.
